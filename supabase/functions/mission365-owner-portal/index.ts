import { createClient } from 'npm:@supabase/supabase-js@2.112.0'
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, apikey, content-type','Access-Control-Allow-Methods':'GET, POST, OPTIONS'}
const json=(body:unknown,status=200)=>Response.json(body,{status,headers:{...cors,'cache-control':'no-store'}})
function publicKey(){const modern=Deno.env.get('SUPABASE_PUBLISHABLE_KEYS');return modern?JSON.parse(modern).default:Deno.env.get('SUPABASE_ANON_KEY')!}
function secretKey(){const modern=Deno.env.get('SUPABASE_SECRET_KEYS');return modern?JSON.parse(modern).default:Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!}
function admin(){return createClient(Deno.env.get('SUPABASE_URL')!,secretKey(),{auth:{persistSession:false,autoRefreshToken:false}})}
function scoped(auth:string){return createClient(Deno.env.get('SUPABASE_URL')!,publicKey(),{auth:{persistSession:false,autoRefreshToken:false},global:{headers:{Authorization:auth}}})}
function slugify(value:string){return value.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,70)||'mission'}
async function requireUser(req:Request){const auth=req.headers.get('authorization')||'';const token=auth.startsWith('Bearer ')?auth.slice(7):'';if(!token)return {error:json({error:'Authentication required'},401)};const {data:{user},error}=await scoped(auth).auth.getUser(token);if(error||!user)return {error:json({error:'Invalid session'},401)};return {user}}
async function memberRole(db:any,userId:string,organizationId:string){const {data}=await db.from('mission365_organization_members').select('member_role').eq('user_id',userId).eq('organization_id',organizationId).maybeSingle();return data?.member_role||null}
async function canAccessApplication(db:any,userId:string,applicationId:string){const {data}=await db.from('mission365_applications').select('id,organization_id').eq('id',applicationId).eq('applicant_user_id',userId).maybeSingle();return data||null}
async function missionOwned(db:any,userId:string,missionId:string){const {data:mission}=await db.from('mission365_missions').select('*').eq('id',missionId).maybeSingle();if(!mission)return null;const role=await memberRole(db,userId,mission.organization_id);return ['owner','manager'].includes(role||'')?mission:null}
async function availableForMission(db:any,missionId:string){const {data:donations}=await db.from('mission365_donations').select('amount_cents,refunded_amount_cents,platform_fee_cents,status').eq('mission_id',missionId).eq('settlement_mode','mission_payout').in('status',['succeeded','partially_refunded']);const {data:prior}=await db.from('mission365_payouts').select('amount_cents').eq('mission_id',missionId).in('status',['pending_review','approved','processing','paid']);const proceeds=(donations||[]).reduce((sum:number,row:any)=>sum+Math.max(0,Number(row.amount_cents)-Number(row.refunded_amount_cents||0)-Number(row.platform_fee_cents||0)),0);const committed=(prior||[]).reduce((sum:number,row:any)=>sum+Number(row.amount_cents||0),0);return Math.max(0,proceeds-committed)}
async function snapshot(db:any,userId:string){const [{data:applications},{data:memberships}]=await Promise.all([db.from('mission365_applications').select('*').eq('applicant_user_id',userId).order('created_at',{ascending:false}),db.from('mission365_organization_members').select('organization_id,member_role,created_at').eq('user_id',userId)]);const orgIds=Array.from(new Set([...(memberships||[]).map((x:any)=>x.organization_id),...(applications||[]).map((x:any)=>x.organization_id).filter(Boolean)])) as string[];const organizations=orgIds.length?(await db.from('mission365_organizations').select('*').in('id',orgIds).order('created_at',{ascending:true})).data||[]:[];const documents=(await db.from('mission365_documents').select('*').eq('uploaded_by',userId).order('created_at',{ascending:false})).data||[];const missions=orgIds.length?(await db.from('mission365_missions').select('*').in('organization_id',orgIds).order('created_at',{ascending:false})).data||[]:[];const missionIds=missions.map((m:any)=>m.id);const payoutAccounts=orgIds.length?(await db.from('mission365_payout_accounts').select('organization_id,onboarding_status,transfers_status,requirements_due,last_checked_at,updated_at').in('organization_id',orgIds)).data||[]:[];const payouts=orgIds.length?(await db.from('mission365_payouts').select('*').in('organization_id',orgIds).order('created_at',{ascending:false})).data||[]:[];const milestones=missionIds.length?(await db.from('mission365_milestones').select('*').in('mission_id',missionIds).order('sort_order',{ascending:true})).data||[]:[];const impactUpdates=missionIds.length?(await db.from('mission365_impact_updates').select('*').in('mission_id',missionIds).order('created_at',{ascending:false})).data||[]:[];return {applications:applications||[],memberships:memberships||[],organizations,documents,missions,payoutAccounts,payouts,milestones,impactUpdates}}
Deno.serve(async(req:Request)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(!['GET','POST'].includes(req.method))return json({error:'Method not allowed'},405);try{const authResult=await requireUser(req);if('error' in authResult)return authResult.error;const user=authResult.user!;const db=admin();if(req.method==='GET')return json(await snapshot(db,user.id));const body=await req.json();const action=String(body?.action||'')
if(action==='initialize_workspace'){const applicationId=String(body.applicationId||'');const {data:application,error}=await db.from('mission365_applications').select('*').eq('id',applicationId).eq('applicant_user_id',user.id).single();if(error||!application)return json({error:'Application not found'},404);if(application.organization_id){const role=await memberRole(db,user.id,application.organization_id);if(!role)await db.from('mission365_organization_members').insert({organization_id:application.organization_id,user_id:user.id,member_role:'owner'});return json({initialized:true,organizationId:application.organization_id,portal:await snapshot(db,user.id)})}const organizationId=crypto.randomUUID();const organizationType=application.application_type==='business_partner'?'business':'community_project';const {error:orgError}=await db.from('mission365_organizations').insert({id:organizationId,legal_name:application.legal_name,public_name:application.public_name,organization_type:organizationType,verification_status:'pending'});if(orgError)throw orgError;const {error:memberError}=await db.from('mission365_organization_members').insert({organization_id:organizationId,user_id:user.id,member_role:'owner'});if(memberError){await db.from('mission365_organizations').delete().eq('id',organizationId);throw memberError}await db.from('mission365_applications').update({organization_id:organizationId,updated_at:new Date().toISOString()}).eq('id',applicationId).eq('applicant_user_id',user.id);await db.from('mission365_audit_log').insert({actor_user_id:user.id,action:'owner.workspace.initialized',entity_type:'organization',entity_id:organizationId,after_state:{verification_status:'pending'},metadata:{application_id:applicationId}});return json({initialized:true,organizationId,portal:await snapshot(db,user.id)})}
if(action==='save_organization'){const organizationId=String(body.organizationId||'');const role=await memberRole(db,user.id,organizationId);if(!['owner','manager'].includes(role||''))return json({error:'Organization owner or manager access required'},403);const {data:current}=await db.from('mission365_organizations').select('verification_status').eq('id',organizationId).single();if(!current||['rejected','suspended'].includes(current.verification_status))return json({error:'Organization profile is locked'},409);const allowedTypes=['nonprofit','community_project','school','faith_organization','business','individual_mission'];const organizationType=String(body.organization_type||'');if(!allowedTypes.includes(organizationType))return json({error:'Unsupported organization type'},400);const patch={legal_name:String(body.legal_name||'').trim(),public_name:String(body.public_name||'').trim(),organization_type:organizationType,website_url:String(body.website_url||'').trim()||null,updated_at:new Date().toISOString()};if(!patch.legal_name||!patch.public_name)return json({error:'Legal and public names are required'},400);const {data,error}=await db.from('mission365_organizations').update(patch).eq('id',organizationId).select('*').single();if(error)throw error;await db.from('mission365_audit_log').insert({actor_user_id:user.id,action:'owner.organization_profile.updated',entity_type:'organization',entity_id:organizationId,after_state:data,metadata:{member_role:role}});return json({organization:data})}
if(action==='register_document'){const applicationId=body.applicationId?String(body.applicationId):null;const organizationId=body.organizationId?String(body.organizationId):null;const missionId=body.missionId?String(body.missionId):null;const milestoneId=body.milestoneId?String(body.milestoneId):null;const impactUpdateId=body.impactUpdateId?String(body.impactUpdateId):null;const documentType=String(body.documentType||'').trim();const storagePath=String(body.storagePath||'').trim();if(!documentType||!storagePath||(!applicationId&&!organizationId&&!missionId))return json({error:'Document type, storage path, and a verification or mission record are required'},400);if(!storagePath.startsWith(`${user.id}/`))return json({error:'Storage path is outside your private user folder'},403);let allowed=false;if(applicationId)allowed=Boolean(await canAccessApplication(db,user.id,applicationId));if(organizationId)allowed=allowed||Boolean(await memberRole(db,user.id,organizationId));if(missionId)allowed=allowed||Boolean(await missionOwned(db,user.id,missionId));if(!allowed)return json({error:'You cannot attach documents to this record'},403);const parts=storagePath.split('/');const fileName=parts.pop()!;const folder=parts.join('/');const {data:objects,error:listError}=await db.storage.from('mission365-private').list(folder,{search:fileName,limit:20});if(listError||!(objects||[]).some((o:any)=>o.name===fileName))return json({error:'Uploaded file could not be verified in private storage'},409);const {data,error}=await db.from('mission365_documents').insert({application_id:applicationId,organization_id:organizationId,mission_id:missionId,milestone_id:milestoneId,impact_update_id:impactUpdateId,uploaded_by:user.id,document_type:documentType,storage_bucket:'mission365-private',storage_path:storagePath,review_status:'pending'}).select('*').single();if(error)throw error;await db.from('mission365_audit_log').insert({actor_user_id:user.id,action:'owner.document.uploaded',entity_type:'document',entity_id:data.id,after_state:{document_type:documentType,review_status:'pending'},metadata:{application_id:applicationId,organization_id:organizationId,mission_id:missionId,milestone_id:milestoneId,impact_update_id:impactUpdateId}});return json({document:data})}
if(action==='save_mission'){const organizationId=String(body.organizationId||'');const role=await memberRole(db,user.id,organizationId);if(!['owner','manager'].includes(role||''))return json({error:'Organization owner or manager access required'},403);const mission=body.mission||{};const title=String(mission.title||'').trim(),summary=String(mission.summary||'').trim(),story=String(mission.story||'').trim(),category=String(mission.category||'').trim();const goalAmountCents=Math.round(Number(mission.goalAmount||0)*100);if(title.length<4||summary.length<20||story.length<40||!category||!Number.isFinite(goalAmountCents)||goalAmountCents<100)return json({error:'Mission needs a title, 20+ character summary, 40+ character story, category, and goal of at least $1'},400);const patch:any={organization_id:organizationId,title,summary,story,category,city:String(mission.city||'').trim()||null,region:String(mission.region||'').trim()||null,country_code:String(mission.countryCode||'US').trim().toUpperCase().slice(0,2)||'US',goal_amount_cents:goalAmountCents,funding_opens_at:mission.fundingOpensAt||null,funding_closes_at:mission.fundingClosesAt||null,updated_at:new Date().toISOString()};let saved:any;if(mission.id){const {data:existing}=await db.from('mission365_missions').select('*').eq('id',String(mission.id)).eq('organization_id',organizationId).maybeSingle();if(!existing)return json({error:'Mission draft not found'},404);if(existing.status!=='draft')return json({error:'Only draft missions can be edited'},409);const {data,error}=await db.from('mission365_missions').update(patch).eq('id',existing.id).select('*').single();if(error)throw error;saved=data}else{const id=crypto.randomUUID();patch.id=id;patch.slug=`${slugify(title)}-${id.slice(0,8)}`;patch.status='draft';const {data,error}=await db.from('mission365_missions').insert(patch).select('*').single();if(error)throw error;saved=data}await db.from('mission365_audit_log').insert({actor_user_id:user.id,action:'owner.mission_draft.saved',entity_type:'mission',entity_id:saved.id,after_state:{status:saved.status,title:saved.title,goal_amount_cents:saved.goal_amount_cents},metadata:{organization_id:organizationId}});return json({mission:saved})}
if(action==='save_milestone'){const missionId=String(body.missionId||'');const mission=await missionOwned(db,user.id,missionId);if(!mission)return json({error:'Mission access required'},403);const ms=body.milestone||{};const title=String(ms.title||'').trim();const description=String(ms.description||'').trim();if(title.length<3||description.length<15)return json({error:'Milestone needs a title and a clear measurable description.'},400);const patch:any={mission_id:missionId,title,description,target_date:ms.targetDate||null,sort_order:Number.isInteger(ms.sortOrder)?ms.sortOrder:0};let saved:any;if(ms.id){const {data:existing}=await db.from('mission365_milestones').select('*').eq('id',String(ms.id)).eq('mission_id',missionId).maybeSingle();if(!existing)return json({error:'Milestone not found'},404);if(existing.verification_status==='verified')return json({error:'Verified milestones cannot be edited.'},409);const {data,error}=await db.from('mission365_milestones').update(patch).eq('id',existing.id).select('*').single();if(error)throw error;saved=data}else{const {data,error}=await db.from('mission365_milestones').insert({...patch,verification_status:'pending'}).select('*').single();if(error)throw error;saved=data}return json({milestone:saved})}
if(action==='submit_milestone'){const milestoneId=String(body.milestoneId||'');const {data:ms}=await db.from('mission365_milestones').select('*').eq('id',milestoneId).maybeSingle();if(!ms)return json({error:'Milestone not found'},404);const mission=await missionOwned(db,user.id,ms.mission_id);if(!mission)return json({error:'Mission access required'},403);const {count:evidence}=await db.from('mission365_documents').select('id',{count:'exact',head:true}).eq('milestone_id',milestoneId);if(!evidence)return json({error:'Submit at least one private evidence document before milestone verification.'},409);const {data:updated,error}=await db.from('mission365_milestones').update({verification_status:'submitted',completed_at:ms.completed_at||new Date().toISOString()}).eq('id',milestoneId).select('*').single();if(error)throw error;return json({milestone:updated})}
if(action==='save_impact'){const missionId=String(body.missionId||'');const mission=await missionOwned(db,user.id,missionId);if(!mission)return json({error:'Mission access required'},403);const update=body.impactUpdate||{};const title=String(update.title||'').trim(),text=String(update.body||'').trim();if(title.length<4||text.length<40)return json({error:'Impact update needs a title and at least 40 characters of detail.'},400);let saved:any;if(update.id){const {data:existing}=await db.from('mission365_impact_updates').select('*').eq('id',String(update.id)).eq('mission_id',missionId).eq('author_user_id',user.id).maybeSingle();if(!existing)return json({error:'Impact update not found'},404);if(existing.status!=='draft')return json({error:'Only draft impact updates can be edited.'},409);const {data,error}=await db.from('mission365_impact_updates').update({title,body:text,milestone_id:update.milestoneId||null}).eq('id',existing.id).select('*').single();if(error)throw error;saved=data}else{const {data,error}=await db.from('mission365_impact_updates').insert({mission_id:missionId,milestone_id:update.milestoneId||null,author_user_id:user.id,title,body:text,status:'draft'}).select('*').single();if(error)throw error;saved=data}return json({impactUpdate:saved})}
if(action==='submit_impact'){const impactUpdateId=String(body.impactUpdateId||'');const {data:update}=await db.from('mission365_impact_updates').select('*').eq('id',impactUpdateId).eq('author_user_id',user.id).maybeSingle();if(!update)return json({error:'Impact update not found'},404);const mission=await missionOwned(db,user.id,update.mission_id);if(!mission)return json({error:'Mission access required'},403);const {count:evidence}=await db.from('mission365_documents').select('id',{count:'exact',head:true}).eq('impact_update_id',impactUpdateId);if(!evidence)return json({error:'Submit at least one evidence document before impact review.'},409);const {data:updated,error}=await db.from('mission365_impact_updates').update({status:'submitted'}).eq('id',impactUpdateId).eq('status','draft').select('*').single();if(error)throw error;return json({impactUpdate:updated})}
if(action==='request_payout'){const missionId=String(body.missionId||'');const mission=await missionOwned(db,user.id,missionId);if(!mission)return json({error:'Mission access required'},403);if(!['published','funded','reporting','completed'].includes(mission.status))return json({error:'Payouts are only available for live or completed missions.'},409);const [{data:org},{data:payout},{count:risks}]=await Promise.all([db.from('mission365_organizations').select('verification_status').eq('id',mission.organization_id).maybeSingle(),db.from('mission365_payout_accounts').select('onboarding_status,transfers_status').eq('organization_id',mission.organization_id).maybeSingle(),db.from('mission365_risk_events').select('id',{count:'exact',head:true}).eq('status','open').in('severity',['high','critical']).or(`mission_id.eq.${missionId},organization_id.eq.${mission.organization_id}`)]);if(org?.verification_status!=='verified'||!payout||payout.onboarding_status!=='ready'||payout.transfers_status!=='active')return json({error:'Payout requests require verified organization and transfer-ready Stripe onboarding.'},409);if(risks)return json({error:'Payout requests are blocked while a high-severity risk review is open.'},423);const amountCents=Math.round(Number(body.amount||0)*100);const available=await availableForMission(db,missionId);if(!Number.isFinite(amountCents)||amountCents<100||amountCents>available)return json({error:'Requested payout exceeds available cleared proceeds.',availableCents:available},409);const {data:created,error}=await db.from('mission365_payouts').insert({mission_id:missionId,organization_id:mission.organization_id,amount_cents:amountCents,platform_fee_cents:0,currency:'usd',status:'pending_review',requested_by:user.id,requested_at:new Date().toISOString()}).select('*').single();if(error)throw error;await db.from('mission365_audit_log').insert({actor_user_id:user.id,action:'owner.payout.requested',entity_type:'payout',entity_id:created.id,after_state:{status:'pending_review',amount_cents:amountCents},metadata:{mission_id:missionId,organization_id:mission.organization_id}});return json({payout:created,availableCents:available-amountCents})}
if(action==='submit_mission'){const missionId=String(body.missionId||'');const mission=await missionOwned(db,user.id,missionId);if(!mission)return json({error:'Mission not found'},404);if(mission.status!=='draft')return json({error:'Only draft missions can be submitted'},409);const [{data:org},{data:payout},{count:milestones},{data:terms}]=await Promise.all([db.from('mission365_organizations').select('verification_status').eq('id',mission.organization_id).single(),db.from('mission365_payout_accounts').select('onboarding_status,transfers_status').eq('organization_id',mission.organization_id).maybeSingle(),db.from('mission365_milestones').select('id',{count:'exact',head:true}).eq('mission_id',missionId),db.from('mission365_terms_acceptances').select('id').eq('user_id',user.id).eq('document_key','mission_owner_terms').eq('version','2026-08-08').maybeSingle()]);if(org?.verification_status!=='verified')return json({error:'Mission submission is locked until the organization is verified'},409);if(!payout||payout.onboarding_status!=='ready'||payout.transfers_status!=='active')return json({error:'Mission submission is locked until payout onboarding is ready and transfer-active'},409);if(!milestones)return json({error:'Add at least one measurable milestone before mission submission.'},409);if(!terms)return json({error:'Accept the current Mission Owner agreement before mission submission.'},428);const {data,error}=await db.from('mission365_missions').update({status:'under_review',submitted_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',missionId).select('*').single();if(error)throw error;await db.from('mission365_audit_log').insert({actor_user_id:user.id,action:'owner.mission.submitted',entity_type:'mission',entity_id:missionId,before_state:{status:'draft'},after_state:{status:'under_review'},metadata:{organization_id:mission.organization_id}});return json({mission:data})}
return json({error:'Unsupported action'},400)}catch(error){console.error('mission365 owner portal failed',error);return json({error:error instanceof Error?error.message:'Owner portal request failed'},500)}})
